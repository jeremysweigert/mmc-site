# mmc.swaggle.com

Public-facing marketing site for Michael's Men's Club AI Operations system.

Built as a single self-contained `index.html` — no build step, no dependencies, no framework.

**To update:** edit `index.html` and push to the `main` branch.  
**Live at:** https://mmc.swaggle.com (via CNAME + GitHub Pages)
